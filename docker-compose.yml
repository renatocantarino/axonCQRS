version: '3.7'
services:
  postgres_cadastro:
    container_name: postgres_bank
    image: postgres:12.2
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"


